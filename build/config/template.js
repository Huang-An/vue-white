module.exports = {
    IMPORT_TEMPLATE: 'import {{name}} from \'../packages/{{package}}/index.js\';',

    INSTALL_COMPONENT_TEMPLATE: '{{name}}',

    MAIN_TEMPLATE: `/* Automatically generated by './build/bin/build-entry.js' */
    
{{include}}

const components = [
    {{install}}
];        
        
const install = function(Vue, opts = {}) {
    components.forEach(component => {
        if (component.name) {
            Vue.component(component.name, component);
        }
    });
            
    Vue.prototype.$opts = {
        size: opts.size || ''
    };
};

/* istanbul ignore if */
if (typeof window !== 'undefined' && window.Vue) {
    install(window.Vue);
}

export default {
    version: '{{version}}',
    install,
    {{list}}
};
`
};